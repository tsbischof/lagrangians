-speedup: check the endpoint every n steps, then seek backwards when we find it to get the first point where the endpoint is satisfied.
-for video run, eliminate the copying involved in working with boost::multi_array
