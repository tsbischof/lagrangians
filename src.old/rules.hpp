#ifndef RULES_HPP_
#define RULES_HPP_

#include <vector>

using namespace std;

int sign(double x);
bool first_turnaround(vector<double> *, vector<double> *);
bool first_flip(vector<double> *, vector<double> *);

#endif
